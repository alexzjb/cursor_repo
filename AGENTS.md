# AGENTS.md

## Cursor Cloud specific instructions

This repository (`cursor_repo`) is currently an empty scaffold containing only a `README.md`.
There is no application code, no dependencies, no build system, no tests, and no linting configuration.

- **No update script is required** — there are no dependencies to install.
- **No services to start** — there is no application to run.
- When code is added to this repository, update this section with instructions for running, testing, and linting the project.
